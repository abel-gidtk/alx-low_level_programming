#include <stdio.h>

/**
 * main - Prints its name followed by a new line
 * @argc: Argument count
 * @argv: Array of arguments
 *
 * Return: void
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
